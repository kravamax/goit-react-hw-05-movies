const NotFoundPage = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        fontSize: '25px',
      }}
    >
      404 | This page could not be found.
    </div>
  );
};

export default NotFoundPage;
